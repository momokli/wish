[deemix]
base_url = "http://localhost:6597"
arl = ""   # injected via WISH_DEEMIX_ARL env var

[download]
output_dir = "{{ download_dir }}"

[spotify]
# credentials come from env vars (WISH_SPOTIFY_CLIENT_ID, WISH_SPOTIFY_CLIENT_SECRET)
